package navigation;

import javafx.stage.Stage;
import model.enums.View;

public class Navigation {
	
	public static void loadPage(Stage primaryStage, View view) {
		if(View.CONTACT_ADD.equals(view)) {
			
		}
	}
}
